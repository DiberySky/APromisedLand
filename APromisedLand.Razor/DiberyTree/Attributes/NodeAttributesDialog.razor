@typeparam TItem where TItem : class, ITreeNodeBase<TItem>, new()

@* @inject DiberyTreeApiClient<TItem> ApiClient *@

<DialogSky Title="节点属性"
           CancelButtonVisible="true"
           SubmitButtonVisible="false"
           OnCanceledClick="@(() => MudDialog?.Close(DialogResult.Ok(true)))">
    <DialogContent>
        <NodeAttributesPanel TItem="TItem" NodeId="@NodeId" />
    </DialogContent>
</DialogSky>

@code {
    [CascadingParameter] public IMudDialogInstance? MudDialog { get; set; }
    [Parameter] public required string NodeId { get; set; }
}